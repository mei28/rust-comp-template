use cmp::{max, min, Reverse};
use proconio::{fastout, input, marker::*};
use std::collections::*;
use std::*;

const INF: usize = 1000000;

fn main() {
    input! {
        n: usize,
    }

    println!("{}", n);
}
